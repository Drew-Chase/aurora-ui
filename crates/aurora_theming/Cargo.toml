[package]
name = "aurora_theming"
version = "0.1.0"
edition = "2024"
description = "Proc macro for compile-time theme configuration from JSON."

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1"
quote = "1"
syn = { version = "2", features = ["full"] }
serde_json = "1"
