[package]
name = "aurora_render"
version = "0.1.0"
edition = "2024"

[dependencies]
aurora_core = { path = "../aurora_core" }
aurora_text = { path = "../aurora_text" , optional = true}


[features]
text = ["dep:aurora_text"]