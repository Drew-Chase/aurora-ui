[package]
name = "image_example"
version = "0.1.0"
edition = "2024"

[dependencies]
aurora_ui = { path = "../../aurora", features = ["software", "image", "svg"] }

[build-dependencies]
winresource = "0.1"
image = { version = "0.25", default-features = false, features = ["png", "ico"] }
